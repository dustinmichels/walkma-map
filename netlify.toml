[build]
  base = "app"
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "../netlify/functions"

[build.environment]
  NODE_VERSION = "22.12.0"

# Redirect rules for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
